SGA(System Global Area)
  锁存器(Latch 闭锁,门插栓):
  A.共享池----遵循LRU算法(最近最少使用算法)
     库高速缓存(Library Cache):存储执行过的每一条SQL语句在共享池中都存有解析后的内容。在解析每一条语句前，Oracle会检查Library cacche是否有同样的语句,以便考虑是否要解析当前的语句。
     数据字典缓存(Dictionary Cache):存放了Oracle所使用的系统参数以及所有的数据库对象信息。
     服务器结果缓存
     保留池
     其他
  B.数据库高速缓存
  C.重做日志缓存
  D.固定系统共享区
  E.大缓冲池
  F.流池
  G.Java池
PGA(Process Global Area)
   定义:
